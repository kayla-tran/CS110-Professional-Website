{
  "dependencies": {
    "three": "^0.164.1"
  },
  "scripts": {
    "build": "npm run deploy",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
